import styled from "@emotion/styled";
export const SearchBoxStyle = styled("div")(() => ({
  border: "2px solid",
  borderColor: "lightgray",
}));
export const InputSearchBoxStyle = styled("input")(() => ({
  border: "none",
  width: "465px",
}));

import { styled } from "@mui/material/styles";

export const FieldStyle = styled("div")(({ theme }) => ({
  paddingBottom: "24px",
}));

export const CheckboxStyle = styled("div")(({ theme }) => ({
  paddingBottom: "24px",
  display: "flex",
  alignItems: "center",
}));
